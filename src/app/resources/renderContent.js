import { person, social, home, about, blog, work, gallery } from './content';

export function renderContent(t) {
    return {
        person,
        social,
        home,
        about,
        blog,
        work,
        gallery
    }
}